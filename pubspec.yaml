name: dios_delices
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=2.18.1'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  simple_animations: ^5.0.0+2
  avatar_glow:
  cupertino_icons: ^1.0.2
  english_words: ^4.0.0
  flutter_launcher_icons: ^0.10.0
  google_fonts: ^8.1.0
  lottie: ^1.4.2
  email_validator: ^2.1.17
  flutter_pw_validator: ^1.6.0
  curved_navigation_bar: ^1.0.3 # non utilisé, conservé pour compatibilité
  shared_preferences: ^2.0.15
  timer_builder: ^2.0.0
  url_launcher: ^6.1.6
  counter: ^0.2.2
  intl: ^0.19.0
  quantity_input: ^1.0.2
  count_stepper: ^0.0.4
  roundcheckbox: ^2.0.5
  font_awesome_flutter: ^10.6.0
  flutter_riverpod: ^2.4.10
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  gpassword: ^0.0.1
  path: ^1.8.3
  http: ^1.2.0
  path_provider: ^2.1.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  parse_server_sdk_flutter: ^10.7.0
  geolocator: ^9.0.2
  geocoding: ^2.0.0
  twilio_flutter: ^0.9.0
  file_picker: ^9.0.0
  cached_network_image: ^3.3.1
  syncfusion_flutter_pdfviewer: ^33.2.4

  flutter_localizations: 
    sdk: flutter   
  pin_code_fields: ^8.0.1
  image_picker: ^1.1.2
  connectivity_plus: ^7.1.1
  confetti: ^0.8.0
  multi_select_flutter: ^4.1.3
  timezone: ^0.9.4
  flutter_local_notifications: ^18.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.3.3

flutter_icons:
  android: true
  ios: true
  image_path_ios: "assets/images/app_icon_plain.png"
  image_path_android: "assets/images/app_icon_plain.png"
  adaptive_icon_background: "#C84C2F"
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubsqpec

dependency_overrides:
  intl: ^0.17.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  generate: true
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/logo_big.jpg
    - assets/images/logo_big.png
    - assets/images/logo_sm01.jpg
    - assets/images/app_icon_plain.png
    - assets/images/logo_sm02.jpg
    - assets/images/logo_sm03.jpg
    - assets/images/logo_sm04.jpg
    - assets/images/round_logo.png
    - assets/images/logo_512.png
    - assets/images/logo-without-bg.png
    - assets/images/no_image.png
    - assets/images/background.jpg
    - assets/images/search.png
    - assets/images/paragraph.png
    - assets/images/meals/beef_steak.jpeg
    - assets/images/meals/chicken_nuggets.jpeg
    - assets/images/meals/chocolate_cake.jpeg
    - assets/images/meals/pancakes.jpeg
    - assets/images/meals/pizza.jpeg
    - assets/images/meals/veggie_tomato_mix.jpeg
    - assets/images/meals/meatballs.jpg
